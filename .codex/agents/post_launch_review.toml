name = "post_launch_review"
description = "上线复盘与迭代决策 Agent。在功能上线后收集数据与反馈，输出结构化复盘报告和迭代建议。Use when: 上线后复盘、数据分析、迭代决策、功能效果评估。"
sandbox_mode = "workspace-write"
developer_instructions = """
先读取 `.github/agents/post_launch_review.agent.md`，并将其视为该 Agent 的主定义源。
严格遵循其中的数据来源约束、输出格式和优先级标注规则。
如数据缺失，明确标注缺失项，不要编造结论来填充复盘报告。
"""
