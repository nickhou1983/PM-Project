---
name: modao-prototype
description: "墨刀原型生成与导入 Skill。通过墨刀 MCP（modao-proto-mcp）将需求或设计说明转化为 HTML 原型，并导入墨刀平台用于产品评审和团队协作。触发条件：(1) 生成墨刀原型，(2) 导入墨刀，(3) 原型图导出到墨刀，(4) 设计说明扩写，(5) modao。"
---

# 墨刀原型生成与导入 Skill

通过墨刀 MCP（`modao-proto-mcp`）将产品需求或页面设计说明转化为 HTML 原型，并导入墨刀个人空间，用于产品评审和团队协作展示。

## 前置依赖

- VS Code 用户级 MCP 配置中需已配置 `modao-proto-mcp`（`@modao-mcp/modao-proto-mcp`）
- 墨刀访问令牌已配置（建议通过环境变量注入，避免明文存储在配置文件中）

> **安全提醒**：不要把 `--token=${MODAO_TOKEN}` 直接写进 Codex MCP 的 `args` 里依赖参数插值。在当前 Codex App/RMCP 环境下，这种写法可能导致 token 没有实际传入子进程。更稳妥的方式是通过 shell 启动命令在运行时展开环境变量，例如：`command = "sh"`，`args = ["-lc", "exec npx -y @modao-mcp/modao-proto-mcp --token \"$MODAO_TOKEN\" --url https://modao.cc"]`。

## 参考文件

| 文件 | 内容 | 何时加载 |
|------|------|----------|
| [modao-workflow-guide.md](references/modao-workflow-guide.md) | 墨刀 MCP 工具调用示例、页面分组策略、输出物管理建议 | 首次使用时加载 |

## 外部 Skill 协作

| Skill | 路径 | 协作关系 |
|-------|------|----------|
| `requirement-doc` | `.agents/skills/requirement-doc/` | 可从其步骤 4 生成的 HTML wireframes 作为输入，或从 PRD 功能需求章节提取页面列表 |

## 工作流

### 步骤 1：确定输入与页面范围

明确原型生成的输入来源和页面范围：

**来源 A：从 PRD 提取**
- 读取 PRD 的「功能需求」和「交互设计」章节
- 提取 P0 核心页面列表
- 按功能链路分组（建议每组 3-5 个页面）

**来源 B：从已有 wireframes 转换**
- 读取 `docs/prd-{项目名}/wireframes/` 下的 HTML 文件
- 选择需要导入墨刀的页面子集

**来源 C：直接描述**
- 用户直接描述页面需求（文字或参考图片）

**分组策略**（建议按功能链路分组，每组 3-5 页）：

| 组别 | 典型页面 |
|------|----------|
| 用户访问链路 | 首页、搜索、详情 |
| 内容生产链路 | 发布、编辑、草稿 |
| 账户链路 | 登录、注册、个人主页、设置 |

### 步骤 2：生成设计说明（可选）

如果输入是简短需求而非详细页面描述，先扩写为结构化设计说明：

1. 调用墨刀 MCP 的 `gen_description` 工具
2. 输入：页面需求文本（或参考图片）
3. 输出：结构化的页面设计说明（包含页面目标、模块布局、交互描述）

> **何时跳过**：如果已有详细的 PRD 交互设计描述或完整的 wireframe HTML，可直接进入步骤 3。

### 步骤 3：生成 HTML 原型

根据设计说明或需求描述生成 HTML 原型：

1. 调用墨刀 MCP 的 `gen_html` 工具
2. 输入：步骤 2 的设计说明，或直接的页面需求描述
3. 输出：HTML 原型代码（及用于导入的 key）
4. 按分组逐组生成，每组完成后再进入下一组

### 步骤 4：导入墨刀

将生成的 HTML 原型导入墨刀个人空间：

1. 调用墨刀 MCP 的 `import_html` 工具
2. 如果步骤 3 返回了 key，优先使用 key 导入
3. 否则传入完整的 HTML 内容导入
4. 导入成功后记录墨刀中的页面位置

### 步骤 5：输出汇总

完成所有页面导入后，汇总输出：

```markdown
## 🎨 墨刀原型导入结果

| 页面 | 状态 | 说明 |
|------|------|------|
| {页面1} | ✅/❌ | {导入结果} |
| {页面2} | ✅/❌ | {导入结果} |

**墨刀空间**：已导入到个人空间
**后续建议**：在墨刀中进一步调整布局和交互细节，确认后将评审结论同步回 PRD
```

## 使用定位

- **墨刀是展示层**：用于评审、协作、快速查看
- **仓库 wireframes 是源**：版本管理、diff、自动化测试的基线仍在仓库中
- 如果在墨刀中做了修改，需要手动将变更同步回仓库 wireframes

## 质量检查清单

- [ ] P0 核心页面已全部导入
- [ ] 每个页面导入后在墨刀中可正常查看
- [ ] 导入结果已汇总输出
